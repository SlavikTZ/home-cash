'use strict';
var phrases;

exports.connect = () => {
    phrases = require('./ru');
}

exports.getPhrases = (name) => {
    if (phrases[name]==="undefined") {
        throw new Error(`Нет такой фразы ${name}`);
    }

    return phrases[name];
}